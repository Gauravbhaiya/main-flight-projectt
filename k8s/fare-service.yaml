apiVersion: apps/v1
kind: Deployment
metadata:
  name: fare-service
  namespace: flight-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fare-service
  template:
    metadata:
      labels:
        app: fare-service
    spec:
      containers:
        - name: fare-service
          image: gauravbhaiya/fare-service:latest
          ports:
            - containerPort: 8095
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql:3306/faredatabase?createDatabaseIfNotExist=true
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: flight-secrets
                  key: MYSQL_USERNAME
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: flight-secrets
                  key: MYSQL_PASSWORD
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: flight-config
                  key: EUREKA_URL
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: flight-config
                  key: KAFKA_SERVERS
---
apiVersion: v1
kind: Service
metadata:
  name: fare-service
  namespace: flight-app
spec:
  selector:
    app: fare-service
  ports:
    - port: 8095
      targetPort: 8095
  type: ClusterIP
